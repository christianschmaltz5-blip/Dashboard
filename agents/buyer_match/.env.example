# Buyer Match — local secrets (copy to .env; .env is gitignored, never commit it)
# The daily job reads the Anthropic key from here to write the "thought of you"
# emails. Without it, the job still runs but uses a plain template email.
# This is the SAME key you paste into the dashboard tools (arecblackhills starred email).
ANTHROPIC_API_KEY=sk-ant-api03-...
